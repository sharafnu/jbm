<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
           http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">

	<!-- This is where we configure Spring-Security -->
	<http auto-config="true" use-expressions="true" 
		access-denied-page="/login.html">

		<intercept-url pattern="/login.html" access="permitAll" />
		<intercept-url pattern="/resources/**" access="permitAll" />
		<intercept-url pattern="/**" access="hasAnyRole('ROLE_ADMIN', 'ROLE_USER')"/>
		
		<form-login login-page="/login.html" authentication-failure-url="/login.html?error=true"
			default-target-url="/home.html" />

		<logout invalidate-session="true" logout-success-url="/login.html" 
			delete-cookies="JSESSIONID,SPRING_SECURITY_REMEMBER_ME_COOKIE"
			logout-url="/logout.html" />
		
<!-- 		<remember-me user-service-ref="accessManagerService" />	 -->

	</http>

<!-- <beans:bean id="accessManagerService"
		class="com.innovazions.jbm.service.impl.AccessManagerService" /> -->
		
	<!-- Declare an authentication-manager to use a custom userDetailsService -->
	<!-- <authentication-manager>
		<authentication-provider user-service-ref="jbmUserDetailsService">
			<password-encoder ref="passwordEncoder" />
		</authentication-provider>
	</authentication-manager>
 -->
 
 	
 	<authentication-manager>
		<authentication-provider user-service-ref="accessManagerService">
			<password-encoder hash="md5">
            	<!-- <salt-source user-property="username"/> -->
            	<salt-source system-wide="jbm"/>
        	</password-encoder> 
		</authentication-provider>
	</authentication-manager>
	
	<!-- Use a Md5 encoder since the user's passwords are stored as Md5 in the 
		database -->
	<beans:bean
		class="org.springframework.security.authentication.encoding.Md5PasswordEncoder"
		id="passwordEncoder" />

	<!-- A custom service where Spring will retrieve users and their corresponding 
		access levels -->
<!-- 	<beans:bean id="jbmUserDetailsService"
		class="com.innovazions.jbm.service.impl.JBMUserDetailsServiceImpl" />
 -->
</beans:beans>
